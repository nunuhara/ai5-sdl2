id: technology.haniwa.ai5-sdl2
runtime: org.freedesktop.Platform
runtime-version: "25.08"
sdk: org.freedesktop.Sdk
command: ai5
finish-args:
  # X11 + XShm access
  - --share=ipc
  - --socket=fallback-x11
  # Wayland access
  - --socket=wayland
  # Filesystem access
  - --filesystem=host
  # OpenGL
  - --device=dri
  # Audio
  - --socket=pulseaudio
  # Gamepad
  - --device=input
cleanup:
  - /lib/libai5.a
modules:
  - SDL2_ttf.json
  - name: ai5-sdl2
    buildsystem: meson
    build-options:
      build-args:
        - --share=network
    sources:
      - type: git
        url: https://github.com/nunuhara/ai5-sdl2.git
  - name: metadata
    buildsystem: simple
    build-commands:
      - mkdir -p /app/share/metainfo
      - install -D technology.haniwa.ai5-sdl2.metainfo.xml /app/share/metainfo/
    sources:
      - type: file
        path: technology.haniwa.ai5-sdl2.metainfo.xml
